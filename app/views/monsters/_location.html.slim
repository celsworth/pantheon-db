= turbo_frame_tag 'locationSelectFrame', 'data-turbo-loader-target': 'locationSelect' do
  .select.is-fullwidth
    = select_tag 'monster[location_id]', options_for_select(locations.map{|l| [l.name, l.id]}, selected_id)
