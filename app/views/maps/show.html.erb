<div class="section">
  <div class="container">
    <div data-controller="elm-maps-show" data-elm-maps-show-flags="<%=
    {
      view: { x: params[:x] || '962',
              y: params[:y] || '728',
              zoom: params[:zoom] || '2' },
      guildMember: guild_member?
    }.to_json %>">
    </div>
  </div>
</div
